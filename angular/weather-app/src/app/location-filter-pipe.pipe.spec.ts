import { LocationFilterPipePipe } from './location-filter-pipe.pipe';

describe('LocationFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
